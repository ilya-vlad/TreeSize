<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WPF.ViewModel"
        xmlns:m="clr-namespace:WPF.Models"
        xmlns:cmd="clr-namespace:WPF.Infrastructure.Command"        
        xmlns:tree="clr-namespace:Aga.Controls.Tree;assembly=Aga.Controls"
        xmlns:service="clr-namespace:WPF.Service"        
        mc:Ignorable="d"
        Title="TreeSize Application" Height="700" Width="800">
    
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Window.Resources>

        <!--<vm:MainWindowViewModel x:Key="ViewModel"/>-->
        <service:ImageConverter x:Key="ImageConverter"/>

    </Window.Resources>
    
    <DockPanel>
        <Ribbon DockPanel.Dock="Top" x:Name="rbMenu">
            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu>
                    <RibbonApplicationMenuItem>
                        <RibbonApplicationMenuItem.Header>
                            <StackPanel Orientation="Horizontal">                                
                                <Image Source="image/close.png"  Margin="0, 0, 5, 0" Width="20"/>
                                <TextBlock Text="Close"/>
                            </StackPanel>
                        </RibbonApplicationMenuItem.Header>
                        <RibbonApplicationMenuItem.Command>
                            <cmd:CloseApplicationCommand/>
                        </RibbonApplicationMenuItem.Command>


                    </RibbonApplicationMenuItem>

                </RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>
            <RibbonTab Name="Tab1" Header="Main">
                <RibbonGroup Name="Group1" Header="Folder" MinWidth="10">
                    <RibbonApplicationMenuItem Name="SelectFolder" ImageSource="image/open.png" Width="48"/>
                    <RibbonMenuButton DataContext="{Binding .}" Label="drive" Width="50" ItemsSource="{Binding Drives}">
                        <RibbonMenuButton.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Command" Value="{Binding DataContext.ScanningFolderCommand,
                                                                    RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RibbonMenuButton}}}"/>
                                <Setter Property="CommandParameter" Value="{Binding}" />
                            </Style>
                        </RibbonMenuButton.ItemContainerStyle>
                    </RibbonMenuButton>
                </RibbonGroup>
                <RibbonGroup>
                    <Button x:Name="button1" Height="30" Background="LightGray" Content="Скан" Click="btn_Click"/>
                </RibbonGroup>

            </RibbonTab>
        </Ribbon>

        <TextBlock Height="20"  DockPanel.Dock="Top">
            <Run Text="Путь: "/>
            <Run Text="{Binding PathRoot}" Foreground="Red"/>            
        </TextBlock>

        <tree:TreeList Name="_tree" DockPanel.Dock="Bottom"> 
           
            <tree:TreeList.Model>
                <vm:MainWindowViewModel/>
            </tree:TreeList.Model>
            <tree:TreeList.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Width="400" Header="Name">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <tree:RowExpander/>
                                        <Image Source="{Binding Converter={StaticResource ImageConverter}}"  Margin="0, 0, 5, 0" Width="20" Height="20"/>                                        
                                        <TextBlock Text="{Binding Name}"></TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Size" Width="100" DisplayMemberBinding="{Binding Size}"/>
                        
                    </GridView.Columns>
                </GridView>
            </tree:TreeList.View>
        </tree:TreeList>

    </DockPanel>
   

</Window>
